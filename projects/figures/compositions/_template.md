# Figure Composition Template

Use this after ideation and before writing or revising a figure spec.

Save each composition artifact as:

- `projects/<project>/figures/compositions/<slug>.md`

## Header

- `slug`:
- `slide`:
- `deck`:
- `status`: `composing | ready-for-spec | archived`

## Inputs

- ideation artifact path:
- selected direction:
- brief path:
- supporting image-board paths:

## Figure Job

- One sentence only.

## Visual Composition

- visual protagonist:
- secondary element(s):
- figure family:
- asset strategy: `native | photo | hybrid`
- overall layout:
- what sits where:
- how the proof/stat text attaches:
- what is big:
- what stays quiet:
- accent use:
- image role:
  - `none | substrate | inset | hero`
- if using stock:
  - chosen candidate(s):
  - crop / treatment note:

## Object Definitions

List the actual objects to render. For each include:

- `name`
- `role`
- `shape / form`
- `content inside it`
- `scale`
- `placement`
- `styling note`

## Attachment Rules

- what text belongs inside objects:
- what text belongs outside objects:
- what must never overlap:
- what can be implied instead of written:

## Failure Mode

- one bad version to avoid:
- why it would fail:

## Spec Notes

- renderer approach:
- family payload notes:
- special media slot notes:
