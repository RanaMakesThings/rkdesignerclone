# Figure Render Brief Template

Use this after composition and before writing a final figure spec.

Save each render brief as:

- `projects/<project>/figures/render-briefs/<slug>.md`

## Header

- `slug`:
- `slide`:
- `deck`:
- `status`: `briefing | ready-for-spec | archived`

## Inputs

- ideation artifact path:
- composition artifact path:
- brief path:
- image-board paths:

## Figure Job

- One sentence only.

## Finalists

List 3 to 5 finalist directions worth taking to spec. For each direction include
the fields below.

### `<Direction Name>`

- `name`:
- one-sentence promise:
- asset strategy: `native | photo | hybrid`
- design stance:
  - `baseline house style | restrained variation | bold variation`
- visual protagonist:
- spatial map:
  - top:
  - middle:
  - bottom:
- object inventory:
  - list the exact objects to render
- object count / rhythm:
  - how many columns, cards, dates, slots, etc.
- copy map:
  - which copy sits inside objects
  - which copy sits outside objects
  - which copy is omitted and only implied
- stat attachment:
  - how the two proof stats attach to the visual
- hierarchy:
  - what dominates first
  - what reads second
  - what stays quiet
- palette / style options:
  - base option:
  - more novel option:
  - where accent is allowed:
- image decision:
  - `none | substrate | inset | hero`
  - if using stock, note candidate choice, crop, and treatment
- styling specifics:
  - borders, lines, labels, shadows, textures, grayscale treatment
- anti-patterns:
  - 2 to 4 concrete ugly versions to reject
- renderer notes:
  - specific payload or component notes for the renderer

## Recommended Order

- strongest direction:
- strongest backup:
- risky but high-upside direction:

## Spec Handoff

- which direction should become the next spec:
- what the spec must lock:
- what can remain flexible at spec time:
