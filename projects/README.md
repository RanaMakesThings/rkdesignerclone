# Projects

Shared project space for figure and pitch-deck work.

Current projects:

- `figures/` - briefs, ideation artifacts, compositions, render briefs, and
  canonical figure specs.
- `pitch-deck/` - deck prototype HTML, workflow notes, and PowerPoint helpers.

If you use Git LFS for large files:

`git lfs pull --include='projects/**'`
