Create a self-contained HTML 1920x1080 pitch-deck slide.

Use the first reference image as the shell to preserve. Keep the header,
subheader, left-side ribbon stack, chamber, cycle, lane labels, and overall
layout very close to that reference. Do not redesign the left side or center.

Use the second reference image only as a tone reference for what a real
clinician artifact eventually looks like. Do not reproduce that full detailed
report. This slide is earlier in the story.

Only redesign the rightmost "Clinician-grade starting point" object and the
immediate handoff coming out of the chamber.

Concept: Structured slate, version B.

Requirements:

- Make the right object feel like a premium clipped clinical slate or index
  note rather than a page.
- Use 2 to 4 strong internal structural bands with different hierarchy, so the
  object feels intentionally organized.
- Make the outgoing teal transition more expressive and resolved than the
  current connector.
- The object should communicate "this is what the clinician can start from"
  without turning into the full Slide 8 product artifact.
- Keep it compact, secondary, and elegant.
- White background, off-white surfaces, restrained teal, rounded corners, soft
  shadow, editorial vector style.
- Avoid generic app chrome, fake buttons, icons, or many tiny lines.

Copy constraints:

- Keep the visible right-side title:
  "Clinician-grade starting point"
- Keep any readable content inside the object to an absolute minimum.

The goal is a stronger right-side payoff object that feels clinically serious
and more distinctive than the current small card.
