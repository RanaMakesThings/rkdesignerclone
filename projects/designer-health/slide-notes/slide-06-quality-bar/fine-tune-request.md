# Slide 6 Fine-Tune Request

Date: 2026-04-03

## Baseline

- Official stamped working folder:
  `projects/designer-health/slide-figures/slide-06-quality-bar`
- Approved working reference:
  `projects/designer-health/slide-figures/slide-06-quality-bar/versions/version-000222--slide-06-v220-thick-border-gemini-v1/preview.png`
- Current draft for this micro-pass:
  `projects/designer-health/slide-figures/slide-06-quality-bar/versions/version-000222--slide-06-v220-thick-border-gemini-v1`

## Locked Regions

- Keep the overall `version-000222` composition and copy hierarchy.
- Keep the centered left-side routing from `version-000222`.
- Keep the right-side output card concept.
- Keep the center cycle / lane-label concept and the compact clinician card.
- Keep this pass scoped to the left spacing / runway and wordmark alignment
  inside the existing `version-000222` structure.
- Keep `version-000221` as a side-reference for the left-side line treatment:
  wider and smoother arcs, thicker connector mass, and the stronger
  teal-to-light gradient read through the incoming paths.

## Requested Change

- Keep the left stack vertically centered as in `version-000222`.
- Move the ribbons farther left so there is more runway between the ribbon tips
  and the cycle.
- Let the white chamber come out a bit farther so the longer curves feel
  smoother, without changing the concept family.
- Center the `Vox` wordmark within the chamber instead of keeping it left of
  center.
- Preserve the cycle, line treatment, and right-side output.
- For future left-side passes, borrow from `version-000221` rather than
  redrawing the routing language from scratch:
  - smoother, wider incoming curves
  - thicker gradient-bearing connector bands
  - a more centered cycle posture inside the chamber
- For this `version-000227` child pass, trim ribbon dead space rather than
  moving the whole left system again:
  - shorten the gray incoming ribbons while keeping their entry tips aligned
  - shorten the teal Vox ribbons at the tail so they stop reading as one hard
    right-hand column
  - keep the existing left-to-cycle routing language, but move the teal line
    launch points to match the shorter tails

## Success Checks

- The five labels remain visually centered relative to the white Vox chamber.
- The ribbons remain readable and keep the improved text placement from
  `version-000222`.
- The gap from ribbon tips to cycle is visibly longer and smoother.
- The `Vox` wordmark reads centered in the chamber.
- The figure stays inside the existing `version-000222` concept family rather
  than resetting to a new structure.

## Center Batch Addendum

- Keep the `version-000222` slide family as the visual anchor for center-object
  ideation.
- Treat the left ribbons, right reasoning rails, and clinician-grade card as
  locked for this batch.
- Limit this exploration to the center object only.
- Evaluate four center concepts:
  - Convergence Chamber
  - Reconciliation Ring
  - Case Frame
  - Prism / Refractor
- For each concept, compare two GPT-5.4 HTML variants and two Gemini HTML
  variants.
- Any new left-side line pass should preserve the smoother/wider feel proven by
  `version-000221`.
- The teal ribbons should now read as stacked directional callouts rather than
  ending in the same rigid column as the gray incoming ribbons.

## Center Reset Addendum

- Use `version-000244` as the visual shell for the next pass.
- Treat the left ribbons, chamber footprint, lane labels, and clinician-grade
  card as locked.
- Abandon the more abstract center-object concepts from the previous ideation
  batch.
- Keep the center as a circle, but add clear directional motion cues so it
  reads as an adaptive loop rather than a static badge.
- Rework only the immediate right-side handoff so the output feels like signal
  is being expressed from the loop before resolving into the three reasoning
  rails.
- Prefer simpler, more literal solutions over new metaphors.

## Center Styling Addendum

- Use `version-000249` as the base for the next micro-pass.
- Keep the outer shell, right-side handoff, ribbons, rails, and card locked.
- Remove the small arrow/tick marks attached to the ring.
- Restyle the ring so it feels more intentional and rotational:
  - use repeating light-to-dark gradient segments
  - make the gradient restart so the ring implies spinning
  - avoid the arbitrary teal-and-gray read
- Replace the inner circle copy with:
  - `Medical knowledge`
  - `Clinical reasoning`
  - `Guided follow-up`
- Compare one OpenAI HTML draft and one Gemini HTML draft only.

## Exact Snapshot Addendum

- Save the user-provided HTML exactly as a new slide-6 version bundle.
- Treat this as an exact snapshot rather than a model-generated variation.
- Keep the pasted ring, ribbon, and chamber geometry unchanged in the stamped
  version.

## Gemini Loop Addendum

- Save the next pasted Gemini variant as another exact snapshot version bundle.
- In the follow-up micro-loop, compare one Gemini-requested revision and one
  Codex-authored revision side by side each round.
- Use `version-000270` as the mutable working draft when the user explicitly
  asks to overwrite it in place instead of stamping a new version.

## Right Payoff Ideation Addendum

- Use `version-000244` as the shell reference for right-side ideation.
- Treat the header, subheader, left ribbon stack, chamber, center loop, and
  lane labels as locked.
- Use the stamped Slide 8 artifact only as a tone / seriousness reference, not
  as a literal object to copy.
- Explore only the rightmost payoff object and immediate handoff.
- Compare three concept families:
  - Emerging brief edge
  - Handoff capsule
  - Structured slate
- Generate two HTML variants per family for Gemini and two HTML variants per
  family for OpenAI.
- The right-side object should feel more clinician-grade and more intentional
  than the current small card, but should not become the full Slide 8 report.

## Right Payoff Refine Addendum

- Use `version-000257` as the main right-side reference for the next pass.
- Use `version-000258` only as a secondary structure reference.
- Treat the header, left ribbons, chamber, center loop, and lane labels as
  locked.
- Keep the rightmost object compact, serious, and clinician-grade.
- Remove the fake-redaction read from the report interior:
  - no heavy dark bars that read like censored text
  - no blacked-out rows
- Preserve what `version-000257` gets right:
  - the overall report silhouette
  - the seriousness and compactness of the payoff object
  - the restrained handoff from the chamber
- Borrow only a small amount from `version-000258`:
  - slightly more varied internal structure
  - a little more column / section differentiation
- Generate a handful of HTML attempts across Gemini and OpenAI without
  resetting the concept family.

## Finalization

- Promoted `version-000271--slide-06-user-html-final-official-v1` as the
  official stamped slide-6 version on 2026-04-03.
- Treat the full slide as approved and locked unless a future request
  explicitly reopens slide 6.
- Canonical mirrored root HTML:
  `projects/designer-health/slide-figures/slide-06-quality-bar/slide-06-quality-bar--version-000271.html`
- Canonical mirrored root PNG:
  `projects/designer-health/slide-figures/slide-06-quality-bar/slide-06-quality-bar--version-000271.png`
