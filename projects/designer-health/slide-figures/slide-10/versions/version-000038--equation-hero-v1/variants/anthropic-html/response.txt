```html
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Recovered Visit Time Creates Real Capacity</title>
<style>
  @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap');

  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  body {
    font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
    background: #f0ede8;
    display: flex;
    align-items: center;
    justify-content: center;
    min-height: 100vh;
    padding: 20px;
  }

  .slide {
    width: 1280px;
    height: 720px;
    background: #faf9f7;
    border-radius: 16px;
    box-shadow: 0 4px 40px rgba(0,0,0,0.12);
    display: flex;
    flex-direction: column;
    padding: 56px 72px 44px;
    position: relative;
    overflow: hidden;
  }

  /* Subtle background texture */
  .slide::before {
    content: '';
    position: absolute;
    top: 0; right: 0;
    width: 480px;
    height: 480px;
    background: radial-gradient(ellipse at top right, rgba(16,185,129,0.04) 0%, transparent 70%);
    pointer-events: none;
  }

  /* Header */
  .header {
    margin-bottom: 6px;
  }

  .header h1 {
    font-size: 32px;
    font-weight: 800;
    color: #0f1923;
    letter-spacing: -0.5px;
    line-height: 1.15;
  }

  .header p {
    font-size: 15px;
    font-weight: 400;
    color: #7a8695;
    margin-top: 7px;
    letter-spacing: 0.01em;
  }

  /* Divider */
  .divider {
    width: 100%;
    height: 1px;
    background: linear-gradient(to right, #e2ddd8, transparent);
    margin: 20px 0 32px;
  }

  /* Main equation area */
  .equation-area {
    flex: 1;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0;
    position: relative;
  }

  /* Left: the trigger */
  .trigger-block {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    justify-content: center;
    padding-right: 48px;
  }

  .trigger-label {
    font-size: 11px;
    font-weight: 600;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: #a0a9b4;
    margin-bottom: 10px;
  }

  .trigger-value {
    font-size: 72px;
    font-weight: 900;
    color: #0d7a5f;
    letter-spacing: -3px;
    line-height: 1;
  }

  .trigger-unit {
    font-size: 16px;
    font-weight: 500;
    color: #4a6670;
    margin-top: 8px;
    letter-spacing: 0.01em;
  }

  /* Arrow connector */
  .connector {
    display: flex;
    align-items: center;
    padding: 0 32px;
    opacity: 0.35;
  }

  /* Middle: assumptions chain */
  .assumptions-block {
    display: flex;
    flex-direction: column;
    gap: 14px;
    padding: 0 40px;
    border-left: 1.5px solid #e2ddd8;
    border-right: 1.5px solid #e2ddd8;
  }

  .assumption-row {
    display: flex;
    align-items: center;
    gap: 10px;
  }

  .assumption-dot {
    width: 5px;
    height: 5px;
    border-radius: 50%;
    background: #b8c4c0;
    flex-shrink: 0;
  }

  .assumption-text {
    font-size: 14px;
    font-weight: 500;
    color: #8a9aa3;
    letter-spacing: 0.01em;
  }

  .assumption-text strong {
    color: #5a6e75;
    font-weight: 600;
  }

  /* Equals */
  .equals-connector {
    padding: 0 36px;
    font-size: 36px;
    font-weight: 300;
    color: #c5cdd4;
    line-height: 1;
  }

  /* Right: hero result */
  .result-block {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    justify-content: center;
    padding-left: 8px;
  }

  .result-label {
    font-size: 11px;
    font-weight: 600;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: #a0a9b4;
    margin-bottom: 6px;
  }

  .result-hero {
    display: flex;
    align-items: flex-end;
    line-height: 1;
    gap: 4px;
  }

  .result-amount {
    font-size: 100px;
    font-weight: 900;
    color: #0f1923;
    letter-spacing: -5px;
    line-height: 0.92;
  }

  .result-period {
    font-size: 22px;
    font-weight: 600;
    color: #4a5568
