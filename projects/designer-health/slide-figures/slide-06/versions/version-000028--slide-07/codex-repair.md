# Slide 6 Codex Repair Consultation

## 1. Failure class

This is a `layout` and `semantic` failure, not a family failure. The slide is
still in the right overall workflow-strip family, but one added object is
breaking the composition and duplicating meaning.

## 2. Stay in current family?

Yes. The clean four-step strip is still the right answer for this slide. The
problem is not that the workflow-strip family is wrong; the problem is that the
floating handoff card introduced a fifth object and started doing slide-7 work
too early.

## 3. Exact next move

Do one targeted revision inside the existing `workflow_strip` family. Remove
the floating handoff brief completely, restore the clean `Structure -> Verify`
connector, and keep the handoff meaning inside the existing `Structure` copy
and internal assembly texture rather than previewing the brief as a separate
object.

## 4. What the next render must change

- delete the floating handoff brief between `Structure` and `Verify`
- keep the strip reading as exactly four primary objects
- strengthen the `Structure -> Verify` connector without adding a new object
- let `Structure` imply a prepared brief through internal rows or wording only
- keep `Verify` fully visible and readable

## 5. What not to do

- do not introduce another bridge object, overlay, or popover
- do not reopen the whole family into a swimlane or product-UI diagram
- do not make slide 6 preview the slide-7 artifact as a standalone card
- do not compensate with icons or extra badges
