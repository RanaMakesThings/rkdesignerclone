Use case: background-extraction. Asset type: isolated pitch-deck figure element. Primary request: return the attached asset almost exactly as-is, but cleaner and higher-resolution. Keep the same rounded panel, centered phone, white secure-link card, bottom baseline, and centered step marker 1. Style/medium: same grayscale editorial illustration style as the reference. Composition/framing: same crop and proportions, full circle visible, no extra line sticking out at the bottom right, no stray marks at the top, background outside the asset as clean and empty as possible. Constraints: do not redesign the layout, keep the Secure-link label verbatim, keep the lock icon, crisp edges, better resolution, suitable for placing into an HTML slide. Avoid: new elements, different text, extra shadows, watermarks, cut-off circle, awkward baseline extension.
