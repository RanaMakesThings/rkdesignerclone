Create a self-contained HTML 1920x1080 pitch-deck slide.

Use the first reference image as the shell to preserve. Keep the header,
subheader, left-side ribbon stack, chamber, cycle, lane labels, and overall
layout very close to that reference. Do not redesign the left side or center.

Use the second reference image only as a tone reference for what a real
clinician artifact eventually looks like. Do not reproduce that full detailed
report. This slide is earlier in the story.

Only redesign the rightmost "Clinician-grade starting point" object and the
immediate handoff coming out of the chamber.

Concept: Structured slate.

Requirements:

- Replace the generic little page card with a compact structured slate.
- It should look like a serious clinical note object reduced to its essential
  anatomy: complaint line, prioritized assessment line, explicit clarify /
  verify line.
- The structure should be clear at a glance, but the object should stay simple
  and elegant.
- The handoff from the chamber should feel like the chamber is resolving signal
  into a cleaner, more ordered state.
- The right object should be more rigid and intentional than the current card.
- White / off-white base, restrained teal accents, soft shadow, premium
  editorial aesthetic.
- No full-page report, no many-box dashboard, no busy document details.

Copy constraints:

- Keep the visible right-side title:
  "Clinician-grade starting point"
- Keep interior text minimal and mostly abstract.

The goal is to show structure and clinical seriousness without prematurely
showing the full product artifact.
