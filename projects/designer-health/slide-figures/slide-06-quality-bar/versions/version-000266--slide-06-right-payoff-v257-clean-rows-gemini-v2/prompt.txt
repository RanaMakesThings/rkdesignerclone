Create a self-contained HTML 1920x1080 pitch-deck slide.

Use the first reference image as the main base. Keep the overall slide shell,
header, subheader, left-side ribbons, chamber, center loop, and lane labels
very close to that reference. Do not redesign the left side or the center.

Use the second reference image as a secondary idea source for varied internal
structure inside the right-side report. Borrow only the sense of multiple
regions or columns, not the washed-out whole-card look.

Only refine the rightmost "Clinician-grade starting point" object and the
immediate handoff from the chamber.

Main goal:

- Keep the strong overall report object from the lead reference.
- Remove the accidental redaction look.
- Mix in a little more structured variation inspired by the second reference.

Requirements:

- Avoid any heavy black bars that look like redactions or censorship.
- Use a cleaner hierarchy of lines and content regions.
- Introduce subtle internal structure variation, such as a secondary narrow
  region, side block, or split assessment area, but keep the object compact.
- The object should still feel like one coherent clinician-grade brief, not a
  dashboard.
- Keep the visible organization simple and purposeful.
- White and off-white base, restrained teal, soft shadow, premium editorial
  feel.
- Do not let the report get too detailed or too UI-like.

The ideal result is `257` but cleaner and less weird, with a small amount of
`258`'s structural sophistication.
