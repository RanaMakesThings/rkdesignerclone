Create a self-contained HTML 1920x1080 pitch-deck slide.

Use the first reference image as the shell to preserve. Keep the header,
subheader, left-side ribbon stack, chamber, cycle, lane labels, and overall
layout very close to that reference. Do not redesign the left side or center.

Use the second reference image only as a tone reference for what a real
clinician artifact eventually looks like. Do not reproduce that full detailed
report. This slide is earlier in the story.

Only redesign the rightmost "Clinician-grade starting point" object and the
immediate handoff coming out of the chamber.

Concept: Handoff capsule, version B.

Requirements:

- The outgoing teal connector should broaden into a compact horizontal artifact
  body, almost like a premium medical dossier capsule.
- The right object should look denser and more meaningful than the current
  little page card, but still much simpler than the real Slide 8 brief.
- Show a cleaner hierarchy than three repeated pills: a headline row, one
  structured content region, and one explicit unresolved / verify cue.
- Make the object feel like a resolved endpoint of the handoff, not a separate
  thing floating nearby.
- White background, restrained teal, subtle gradients, soft shadow, premium
  industrial-design vibe.
- Do not add fake icons, charts, or filler UI.

Copy constraints:

- Keep the visible right-side title:
  "Clinician-grade starting point"
- Minimize readable interior text.

The goal is a stronger right-side payoff object whose shape itself communicates
"usable clinical output."
