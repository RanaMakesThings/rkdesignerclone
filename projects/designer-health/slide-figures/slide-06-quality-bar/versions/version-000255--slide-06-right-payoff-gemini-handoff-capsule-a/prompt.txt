Create a self-contained HTML 1920x1080 pitch-deck slide.

Use the first reference image as the shell to preserve. Keep the header,
subheader, left-side ribbon stack, chamber, cycle, lane labels, and overall
layout very close to that reference. Do not redesign the left side or center.

Use the second reference image only as a tone reference for what a real
clinician artifact eventually looks like. Do not reproduce that full detailed
report. This slide is earlier in the story.

Only redesign the rightmost "Clinician-grade starting point" object and the
immediate handoff coming out of the chamber.

Concept: Handoff capsule.

Requirements:

- Instead of a page-like card, let the outgoing teal signal widen and resolve
  into a compact premium capsule or slab.
- The capsule should feel like a condensed clinician-ready artifact: one strong
  headline band, one prioritized middle band, one verify / open-question band.
- It should feel integrated with the handoff rather than floating separately.
- The transition from chamber to object should be the star: signal becoming
  something clinician-usable.
- Keep it compact and elegant, not dashboard-like.
- Use rounded geometry, white and off-white surfaces, restrained teal accents,
  and soft shadow.
- Avoid making it look like a phone, a chat bubble, or a generic UI card.

Copy constraints:

- Keep the visible right-side title:
  "Clinician-grade starting point"
- Keep interior text minimal and mostly abstracted.

The goal is to replace the weak little page card with a more intentional
artifact-form that still reads as clinician-grade output.
